version: "3.7"

services:
  test:
    build:
      context: ../..
      target: test
    environment:
      REPO_BASE: "https://raw.githubusercontent.com/Amsterdam/amsterdam-schema/brk2_active_dataset"
    command: >
      bash -c "./test.sh"
